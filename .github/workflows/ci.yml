name: CI/CD Pipeline for lorencDedaj/NeatNest
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "hello from osp v2"